# Results

`paper_reported_table6.csv` records the values reported in the current manuscript. It is a reference table, not a generated output file.

Running `experiments/run_ablation_lolv1.m` creates a separate `results/lolv1_ablation/` directory containing detailed per-image outputs and recomputed summary statistics.
